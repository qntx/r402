[package]
name = "r402-mcp"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Model Context Protocol (MCP) transport for the x402 payment protocol."
readme = "README.md"
keywords = ["x402", "payment", "mcp", "ai", "http-402"]
categories = [
    "api-bindings",
    "web-programming",
    "asynchronous",
]

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[features]
default = []

telemetry = ["r402-core/telemetry", "dep:tracing"]
server = []
client = []

full = ["telemetry", "server", "client"]

[dependencies]
r402-core = { workspace = true }
tracing = { workspace = true, optional = true }

[lints]
workspace = true
