# x402 Facilitator Configuration
#
# Environment variable expansion: use $VAR or ${VAR} in string values.
# Override host/port with HOST and PORT environment variables.

host = "0.0.0.0"
port = 4021
deploy_erc4337_with_eip6492 = false

[chains."eip155:8453"]
rpc_url = "https://mainnet.base.org"
signer_private_key = "$SIGNER_KEY_BASE"

[chains."eip155:84532"]
rpc_url = "https://sepolia.base.org"
signer_private_key = "$SIGNER_KEY_BASE_SEPOLIA"

[chains."eip155:137"]
rpc_url = "https://polygon-rpc.com"
signer_private_key = "$SIGNER_KEY_POLYGON"
