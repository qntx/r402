[package]
name = "r402-hedera"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Hedera chain support for the x402 payment protocol."
keywords = ["x402", "payment", "hedera", "hts", "usdc"]
categories = [
    "api-bindings",
    "cryptography::cryptocurrencies",
    "web-programming",
    "asynchronous",
]

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[features]
default = []
telemetry = ["dep:tracing", "dep:tracing-core"]
client = ["dep:hedera", "dep:r402-client"]
server = ["dep:r402-server"]
facilitator = [
    "dep:hedera",
    "dep:hex",
    "dep:r402-facilitator",
    "dep:reqwest",
]
full = ["telemetry", "client", "server", "facilitator"]

[dependencies]
base64 = { workspace = true }
compact_str = { workspace = true }
r402-protocol = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

hedera = { workspace = true, optional = true }
hex = { workspace = true, optional = true }
r402-client = { workspace = true, optional = true }
r402-facilitator = { workspace = true, optional = true }
r402-server = { workspace = true, optional = true }
reqwest = { workspace = true, optional = true }
tracing = { workspace = true, optional = true }
tracing-core = { workspace = true, optional = true }

[dev-dependencies]
reqwest = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt"] }
wiremock = { workspace = true }

[lints]
workspace = true
